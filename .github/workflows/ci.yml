name: CI

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  android-ci:
    name: Android Unit Tests & Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: false

      - name: Grant execute permission for gradlew
        run: chmod +x FlexiFeed/gradlew

      - name: Run Android Unit Tests
        run: |
          cd FlexiFeed
          ./gradlew testDevDebugUnitTest --no-daemon --stacktrace

      - name: Assemble Dev Debug APK
        run: |
          cd FlexiFeed
          ./gradlew assembleDevDebug --no-daemon --stacktrace

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-results
          path: FlexiFeed/app/build/reports/tests/testDevDebugUnitTest/
          retention-days: 7

      - name: Upload Dev Debug APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: app-dev-debug
          path: FlexiFeed/app/build/outputs/apk/dev/debug/*.apk
          retention-days: 7

  server-ci:
    name: Node.js Server & DSL Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: server/package-lock.json

      - name: Install dependencies
        run: |
          cd server
          npm ci || npm install

      - name: TypeScript Build & Type Check
        run: |
          cd server
          npm run build
          npm run typecheck

      - name: Validate SDUI JSON DSL & Presets
        run: |
          cd server
          npm test
          node -e "
            const fs = require('fs');
            const path = require('path');
            const dir = 'data';
            const files = ['feed.json', 'presets/mega-sale.json', 'presets/tech-weekend.json'];
            files.forEach(f => {
              const fullPath = path.join(dir, f);
              const data = JSON.parse(fs.readFileSync(fullPath, 'utf-8'));
              if (!data.screen || !data.sections || !Array.isArray(data.sections)) {
                throw new Error(`Invalid SDUI DSL schema in ${f}`);
              }
              if (!data.theme || !data.theme.primaryColor || !data.theme.logo) {
                throw new Error(`Missing theme or logo object in ${f}`);
              }
              console.log(`✔ Valid SDUI DSL & Theme: ${f} (${data.sections.length} sections, logoBg: ${data.theme.logo.bgColor})`);
            });
          "
